#pragma once

// Copia este archivo como credentials.h y rellena tus datos.
// credentials.h está en .gitignore — nunca se commitea.

#define WIFI_SSID  "tu-red-wifi"
#define WIFI_PASS  "tu-contraseña"
#define API_URL    "https://TUDOMINIO.COM"   // sin slash final
