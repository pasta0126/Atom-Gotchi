<!-- Añadir estos permisos al AndroidManifest generado por `flutter create` -->
<!-- Dentro de <manifest> pero FUERA de <application> -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE (API >= 31 = Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- BLE (API < 31 = Android 11 e inferior) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>

    <!-- Batería -->
    <uses-permission android:name="android.permission.BATTERY_STATS"/>

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <application
        android:label="AtomGotchi"
        android:icon="@mipmap/ic_launcher">
        <!-- Resto de la configuración de Flutter aquí -->
    </application>
</manifest>
